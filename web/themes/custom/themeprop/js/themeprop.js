/**
 * @file
 * themeprop behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.themeprop = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
